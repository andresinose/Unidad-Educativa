# Copy to infra/.env and fill in before running docker compose.
# Required for the resource-generation step (/generate) — without it the API
# still runs and /analyze works, but generation returns a clear 503 error.
ANTHROPIC_API_KEY=

# Optional overrides
GENERATION_MODEL=claude-sonnet-5
